[package]
name = "bingrep-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
regex = "1.10"
hex = "0.4"

[dev-dependencies]
uuid = { version = "1.0", features = ["v4"] }
tempfile = "3.8"
